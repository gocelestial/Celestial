enum LogLevels {
    // Highest to lowest importance
	error = "error",
	warn = "warn",
	info = "info",
	http = "http",
	verbose = "verbose",
	debug = "debug",
	silly = "silly",
}

export { LogLevels };
