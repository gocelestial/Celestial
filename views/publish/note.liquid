{% layout 'default' %}

{% if error %}
    <p class="notification notification--error">An error occured: {{ error }}</p>
{% endif %}

<div class="publishing-form publishing-form--note">
    <form action="/publish/note/" method="post">

        <fieldset class="publishing-form__when">
            <legend>Local date time&nbsp;</legend>

            <div
                class="publishing-form__when--part publishing-form__when--date"
            >
                <label for="date">Date:</label>
                <input
                    id="date"
                    name="date"
                    type="date"
                    value={{formDefaults.published.date}}
                />
            </div>

            <div
                class="publishing-form__when--part publishing-form__when--time"
            >
                <label for="time">Time:</label>
                <input
                    id="time"
                    name="time"
                    type="time"
                    value={{formDefaults.published.time}}
                />
            </div>
        </fieldset>

        <fieldset class="publishing-form__content">
            <legend>Your content&nbsp;</legend>

            <label for="note">Note:</label>
            <textarea name="note" id="note" cols="30" rows="10" placeholder="Your note..."></textarea>

            {% if media-endpoint %}
            <label for="media-endpoint">Attach a file:</label>
            <input type="file" id="media-endpoint" name="file">
            {% endif %}
        </fieldset>
        

        {% if syndicate-to %}
        <fieldset class="publishing-form__syndication-targets">
            <legend>Syndicate to&nbsp;</legend>

            {% for link in syndicate-to %}
            <div class="publishing-form__syndication-target">
                <label for="{{link.uid}}">{{link.name}}</label>
                <input type="checkbox" id="{{link.uid}}" name="mp-syndicate-to" />
            </div>
            {% endfor %}
        </fieldset>
        {% endif %}

    
        <input type="hidden" name="h" value="entry">
    
        <button type="submit">Create note</button>
    
    </form>
</div>

